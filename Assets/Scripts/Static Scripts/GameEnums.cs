public static class GameEnums
{
    public enum StoryPathType
    {
        Linear,
        Branching,
        Conditional
    }

    public enum Difficulty
    {
        Easy,
        Normal,
        Hard
    }
}
